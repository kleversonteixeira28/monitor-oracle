apiVersion: 1

apps:
  # Sem isto o plugin instala mas fica desligado, e o datasource Zabbix
  # aparece como "plugin nao encontrado".
  - type: alexanderzobnin-zabbix-app
    org_id: 1
    disabled: false
